
const PATH_ROOT = __dirname;
const PATH_RUNTIME = `${PATH_ROOT}/runtime`;

const PATHS = {
	PATH_ROOT,
	PATH_RUNTIME
};

const QUERY_PATHS = {
	pathOpen : '/path-open',
	pathSave : '/path-save',
	accessFileSave : '/access-file-save',
};

module.exports = {
	QUERY_PATHS,
	PATHS
};

